services:
  - type: worker
    name: supabase-audio-watcher
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python3 sales_pipeline_SUPABASE_WATCHER.py
    autoDeploy: true
